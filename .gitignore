# macOS
.DS_Store

# 编辑器 / IDE
.idea/
.vscode/
*.swp

# Claude Code 的本地设置（机器相关，不该进版本库）
.claude/settings.local.json

# go build 产出的可执行文件（默认落在当前目录，名字同 cmd/ 子目录）
/go/hello
/go/ptrdemo
/go/slicedemo
/go/embeddemo
/go/ifacedemo
/go/loganalyze

# 覆盖率报告
coverage.out

# D19：chart 里的迁移副本由 make helm-sync 生成，源头是 go/migrations/
go/deploy/chart/golearn-api/files/migrations/
